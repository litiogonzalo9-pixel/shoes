version: "3.8"
services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DATABASE_URL=./database.sqlite
    volumes:
      - ./database.sqlite:/app/database.sqlite
    restart: unless-stopped
